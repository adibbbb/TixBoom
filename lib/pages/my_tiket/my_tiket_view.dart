import 'package:flutter/material.dart';

class MyTiketView extends StatelessWidget {
  const MyTiketView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('INI MY TIKET'),
      ),
    );
  }
}
